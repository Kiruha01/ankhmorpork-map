# Vite + TypeScript + MapLibre GL JS + GeoJSON

## Запуск

```bash
npm install
npm run dev
```

Пример GeoJSON расположен в `src/main.ts`. Он добавляется как источник `sample-geojson` и отображается отдельными слоями для точек, линий и полигонов.
